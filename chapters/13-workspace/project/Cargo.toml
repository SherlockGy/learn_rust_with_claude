# Workspace 示例配置
# 这是一个演示文件，实际使用时需要创建子项目

[workspace]
members = [
    # "echo-rs",
    # "word-count",
    # "task-cli",
]

# 共享依赖版本
[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
clap = { version = "4", features = ["derive"] }
